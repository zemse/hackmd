//! `hackmd tui` — stub. Real implementation lands in M6 behind `--features tui`.

use crate::error::Result;

pub fn run() -> Result<()> {
    println!("tui not yet implemented (coming in v0.0.3)");
    Ok(())
}
