# hackmd-rs

## Git

- Always commit AND push after completing a unit of work. Don't wait to be asked — land each self-contained change as its own commit and `git push` to `origin/main` right away. (Work directly on `main`; no worktree.)
